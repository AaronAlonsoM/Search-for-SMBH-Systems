[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "blazars_tools"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "requests",
    "matplotlib",
    "scipy",
    "datetime",
    "ftperiodogram",
    "pyts"
]